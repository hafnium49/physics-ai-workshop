[project]
name = "physics-ai-workshop"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "mujoco>=3.6.0",
    "fastmcp>=2.0",
    "mediapy>=1.2.6",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
